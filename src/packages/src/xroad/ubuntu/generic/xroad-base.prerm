#!/bin/bash

set -e

echo "prerm script executing"
echo "Printing parameters"
echo $@

if [ "$1" = "upgrade" ]; then
  echo "prerm script upgrade"
  exit 1
elif [ "$1" = "failed-upgrade" ]; then
  echo "prerm script failed-upgrade"
  exit 1
else
  echo "no match for the parameter"
  echo "$1"
fi
